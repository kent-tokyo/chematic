# WASM artifact size — v1.0.8

This record measures the optimized `chematic-wasm` artifact built from the
v1.0.8 release candidate after the workspace version and dependency pins were
updated.

| Artifact | Bytes | Decimal size |
|---|---:|---:|
| `chematic_wasm_bg.wasm` | 3,576,000 | 3.58 MB |
| gzip-compressed artifact | 1,305,765 | 1.31 MB |

SHA-256 of the raw WASM artifact:

```text
2389860d51b926c738779cfe641e1f0bb9d10979b0634f18c77bcf2da1828d9b
```

Toolchain: `wasm-pack 0.13.1`, `wasm-opt 130`, macOS arm64, release profile.
The output was produced with `wasm-pack build --target nodejs --release`;
`wasm-pack` ran Binaryen optimization. Size varies with the Rust and Binaryen
toolchains, target, and enabled features.

To reproduce:

```bash
cd crates/chematic-wasm
wasm-pack build --target nodejs --release -d /tmp/chematic-wasm-v1.0.8
wc -c /tmp/chematic-wasm-v1.0.8/chematic_wasm_bg.wasm
gzip -c -9 /tmp/chematic-wasm-v1.0.8/chematic_wasm_bg.wasm | wc -c
shasum -a 256 /tmp/chematic-wasm-v1.0.8/chematic_wasm_bg.wasm
```
