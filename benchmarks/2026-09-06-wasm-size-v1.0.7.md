# WASM artifact size — v1.0.7

This record measures the optimized `chematic-wasm` artifact built from the
`v1.0.7` tag (`1482a4b6c7ce3346a7f0bea65e51b45571ffb7f5`). It supersedes the
v1.0.2-candidate size snapshot for current-release documentation.

| Artifact | Bytes | Decimal size |
|---|---:|---:|
| `chematic_wasm_bg.wasm` | 3,539,396 | 3.54 MB |
| gzip-compressed artifact | 1,290,357 | 1.29 MB |

SHA-256 of the raw WASM artifact:

```text
d9d6e81819b0c5ec0e7afe2e655570271a3fd6d8a88cc292f40947e641afa0f0
```

Toolchain: `wasm-pack 0.13.1`, `wasm-opt 130`, macOS arm64, release profile.
The output was produced with `wasm-pack build --target nodejs --release`;
`wasm-pack` ran Binaryen optimization. Size varies with the Rust and Binaryen
toolchains, target, and enabled features.

To reproduce:

```bash
cd crates/chematic-wasm
wasm-pack build --target nodejs --release -d /tmp/chematic-wasm-pkg
wc -c /tmp/chematic-wasm-pkg/chematic_wasm_bg.wasm
gzip -c -9 /tmp/chematic-wasm-pkg/chematic_wasm_bg.wasm | wc -c
shasum -a 256 /tmp/chematic-wasm-pkg/chematic_wasm_bg.wasm
```
