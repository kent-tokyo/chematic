# WASM artifact size — v1.0.9

Status: local release-candidate evidence. This is an artifact-size measurement,
not a runtime-performance claim.

Environment: macOS arm64, `wasm-pack 0.13.1`, `wasm-opt 130`, release profile,
`--target web`, workspace version `1.0.9`.

| Artifact | Bytes |
|---|---:|
| Raw `schematic_wasm_bg.wasm` | 3,577,593 |
| gzip -9 | 1,306,377 |

SHA-256: `c80f507609b4cb659a1c68793cf053d8726a3c321fa3bd148f78eef349a9cad0`

Reproduction:

```text
wasm-pack build crates/chematic-wasm --target web --release -d demo/pkg
wc -c demo/pkg/chematic_wasm_bg.wasm
gzip -c -9 demo/pkg/chematic_wasm_bg.wasm | wc -c
shasum -a 256 demo/pkg/chematic_wasm_bg.wasm
```

The previous v1.0.8 measurement remains available as a dated historical
comparison; size can vary with source, toolchain, and optimization environment.
