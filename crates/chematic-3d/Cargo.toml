[package]
name = "chematic-3d"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "3D coordinate generation and force-field minimization for chematic — pure Rust"
keywords = ["cheminformatics", "3d", "coordinates", "pdb", "chemistry"]
categories = ["science"]

[dependencies]
chematic-core = { path = "../chematic-core", version = "0.1.0" }
chematic-perception = { path = "../chematic-perception", version = "0.1.0" }

[dev-dependencies]
chematic-smiles = { path = "../chematic-smiles" }
