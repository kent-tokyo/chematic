[package]
name = "chematic-chem"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Molecular descriptors and chemical properties for chematic — pure Rust"
keywords = ["cheminformatics", "chemistry", "descriptors", "rdkit", "lipinski"]
categories = ["science"]

[dependencies]
chematic-core = { path = "../chematic-core", version = "0.1.0" }
chematic-perception = { path = "../chematic-perception", version = "0.1.0" }

[dev-dependencies]
chematic-smiles = { path = "../chematic-smiles" }
