[package]
name = "chematic-rxn"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Reaction SMILES/SMIRKS parser and writer for chematic — pure-Rust RDKit alternative"
keywords = ["reaction", "smiles", "smirks", "cheminformatics", "chemistry"]
categories = ["science", "parser-implementations"]
readme = "../../README.md"
homepage = "https://github.com/kent-tokyo/chematic"
documentation = "https://docs.rs/chematic-rxn"

[dependencies]
chematic-core   = { path = "../chematic-core",   version = "0.1.7" }
chematic-smiles = { path = "../chematic-smiles", version = "0.1.7" }
chematic-smarts = { path = "../chematic-smarts", version = "0.1.7" }
