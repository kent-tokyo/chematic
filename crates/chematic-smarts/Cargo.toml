[package]
name = "chematic-smarts"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "SMARTS query language and VF2 subgraph isomorphism for chematic — pure Rust"
keywords = ["smarts", "cheminformatics", "substructure", "vf2", "chemistry"]
categories = ["science", "parser-implementations"]

[dependencies]
chematic-core = { path = "../chematic-core", version = "0.1.0" }
chematic-perception = { path = "../chematic-perception", version = "0.1.0" }

[dev-dependencies]
chematic-smiles = { path = "../chematic-smiles" }
