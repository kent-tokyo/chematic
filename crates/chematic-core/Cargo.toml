[package]
name = "chematic-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Core types (Atom, Bond, Molecule) for chematic — pure-Rust RDKit alternative, WASM-compatible"
keywords = ["cheminformatics", "chemistry", "molecule", "smiles", "wasm"]
categories = ["science", "science::computational-chemistry::cheminformatics", "data-structures"]
readme = "README.md"
homepage.workspace = true
documentation = "https://docs.rs/chematic-core"

[package.metadata.docs.rs]
rustdoc-args = ["--cfg", "docsrs"]

# 外部依存ゼロ: pure Rust, no FFI
[dependencies]
